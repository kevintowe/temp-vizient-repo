# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build deployments-testing-app`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t deployments-testing-app`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system deployments-testing-app && \
          adduser --system -G deployments-testing-app deployments-testing-app

COPY dist/devops/deployments-testing-app deployments-testing-app
RUN chown -R deployments-testing-app:deployments-testing-app .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix deployments-testing-app --omit=dev -f install

CMD [ "node", "deployments-testing-app" ]
